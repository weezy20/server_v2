//! Main web server that incorporates a main() function, socket server, handler
//! and router, and manages the coordinations among them. It serves as both a web server
//!(serving html) and a web service(serving json).

fn main(){
    
}